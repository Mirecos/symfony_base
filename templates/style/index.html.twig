{% extends 'base.html.twig' %}

{% block title %}{% trans %}Style{% endtrans %}{% endblock %}

{% block body %}
<div class="example-wrapper">
    <a href="{{ path('app_style_creation') }}">{% trans %}Add.a.Style{% endtrans %}</a>
    <ul>
        {% for style in styles %}
            <li> {{ style.name }} </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
